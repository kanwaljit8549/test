_schema-version: "3.2"
ID: gwsp1tempproject
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: gwsp1tempproject-destination-content
  type: com.sap.application.content
  requires:
  - name: gwsp1tempproject-destination-service
    parameters:
      content-target: true
  - name: gwsp1tempproject-repo-host
    parameters:
      service-key:
        name: gwsp1tempproject-repo-host-key
  - name: gwsp1tempproject-uaa
    parameters:
      service-key:
        name: gwsp1tempproject-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: gwsp1tempproject_html_repo_host
          ServiceInstanceName: gwsp1tempproject-html5-srv
          ServiceKeyName: gwsp1tempproject-repo-host-key
          sap.cloud.service: gwsp1tempproject
        - Authentication: OAuth2UserTokenExchange
          Name: gwsp1tempproject_uaa
          ServiceInstanceName: gwsp1tempproject-xsuaa-srv
          ServiceKeyName: gwsp1tempproject-uaa-key
          sap.cloud.service: gwsp1tempproject
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: gwsp1tempproject-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: gwsp1tempproject-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - gwsp1tempproject.zip
      name: gwsp1tempproject
      target-path: resources/
- name: gwsp1tempproject
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: gwsp1tempproject-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: gwsp1tempproject-destination-service
    service-plan: lite
- name: gwsp1tempproject-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: gwsp1tempproject-xsuaa-srv
    service-plan: application
- name: gwsp1tempproject-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: gwsp1tempproject-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
